/**
 * The gba-kit extensions to the Debug Adapter Protocol, re-exported from
 * `@gba-kit/debug-core/protocol` where they are defined: the adapter is one
 * host that answers them, so a DAP client keeps importing them from here.
 */
export * from '@gba-kit/debug-core/protocol';
